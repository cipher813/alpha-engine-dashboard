# marketing-apex/ — Nous Ergon lab landing page

Source for `nousergon.ai` (the apex lab landing: hero, product cards, brand About,
contact). Sibling of `marketing/` (the Crucible product site) — same Astro 6 +
Tailwind 4 + Biome hygiene baseline, separate Cloudflare Pages project.

Plan: `alpha-engine-config/private-docs/site_restructure_plan_260612.md` (config#905).

```sh
cd marketing-apex/
npm install
npm run dev      # http://localhost:4321
npm run build    # astro check && astro build → dist/
```
